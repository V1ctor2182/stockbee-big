# Stockbee 技术设计 — 系统架构总览

**版本**: 2.3
**日期**: 2026-04-19
**作者**: Stockbee 研发团队

---

## 1. 设计哲学

Stockbee 是一个 AI 驱动的量化交易系统，核心特点是 **LLM Agent 作为最终决策编排者**，整合宏观分析、多因子评分、强化学习组合优化、LLM 新闻分析，经过验证层后输出交易建议，人工审批后执行。

系统采用 Provider 抽象模式（借鉴 Qlib 的五大核心 Provider），允许数据源在不同环境下（回测/实盘）灵活切换，同时通过多层缓存最小化 API 调用成本。分层设计保证了各模块独立性，支持增量迭代。→ 参考：research-provider-design.md §1-§2

---

## 2. 分层架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      外部数据源                                   │
│  (Alpaca API / Yahoo Finance / NewsAPI / FRED / Polygon.io)     │
└────────────────────────┬────────────────────────────────────────┘
                         │
        ┌────────────────▼────────────────┐
        │      数据层 (Data Layer)         │
        │  SQLite (WAL) | Parquet | Buffer │
        │  知识图谱 (NetworkX) | 10 Provider│
        └────────────────┬────────────────┘
                         │
        ┌────────────────▼────────────────┐
        │    特征层 (Feature Layer)        │
        │  因子计算引擎 | 表达式评估器     │
        │  MacroTiltEngine | 传播引擎      │
        └────────────────┬────────────────┘
                         │
        ┌────────────────▼────────────────┐
        │    模型层 (Model Layer)          │
        │  LLM 路由 | 小模型 | RL Agent   │
        │  DQN+PPO | SAC+Ensemble        │
        └────────────────┬────────────────┘
                         │
        ┌────────────────▼────────────────┐
        │   LLM Agent 决策层               │
        │  ┌─────────────────────────┐     │
        │  │ 验证层 (CRITICAL)        │     │
        │  │ Schema + 风控 + 异常检测 │     │
        │  └─────────────────────────┘     │
        │  周度再平衡 | 实时监控 | 审批    │
        │  决策日志（输入/输出全记录）      │
        └────────────────┬────────────────┘
                         │
        ┌────────────────▼────────────────┐
        │   执行层 (Execution Layer)       │
        │  Alpaca API | 风控 | 日志记录   │
        │  幂等执行 | 防守机制             │
        │  做空模式: 融券/反向ETF/仅做多   │
        └────────────────┬────────────────┘
                         │
        ┌────────────────▼────────────────┐
        │  仪表板与监控 (Dashboard)        │
        │  训练可视化 | 交易历史 | 告警   │
        │  实时 WebSocket | 审批界面      │
        └─────────────────────────────────┘
```

---

## 3. 数据 Pipeline 全景 — 从数据源到决策

```
═══════════════════════════════════════════════════════════════════════════════
                        STOCKBEE 数据 PIPELINE 全景
═══════════════════════════════════════════════════════════════════════════════

  数据源 (Raw)              处理 (Processing)           流向        决策输入
  ──────────────           ──────────────────          ──────      ──────────

  ┌─── A. 市场价格数据 ──────────────────────────────────────────────────────┐
  │                                                                          │
  │  Alpaca OHLCV ──► Parquet ──► Alpha158 表达式引擎 ──► 158 技术因子       │
  │  (4000只×5年)      (列存)      (MA, RSI, MACD,        (标准化)           │
  │  日度同步                       Bollinger, 相对强度)                      │
  │                                       │                                  │
  │                                       ├──────► MC: 因子融合 → RL 选股    │
  │                                       └──────► 训练: RL 环境状态空间     │
  │                                                                          │
  │  Alpaca OHLCV ──► 实时价格监控 ──► 3σ 异常检测 ──────► FC: 防守平仓触发  │
  │  (当前持仓)        (交易时段)      (ATR×1.5)                              │
  │                                                                          │
  └──────────────────────────────────────────────────────────────────────────┘

  ┌─── B. 股票池元数据 ──────────────────────────────────────────────────────┐
  │                                                                          │
  │  Alpaca 全美股 ──► SQLite broad_universe ──► 四层漏斗筛选                │
  │  (8000只)           (ticker, sector,          8000→4000→500→100          │
  │  每周同步             market_cap,                    │                    │
  │                       avg_volume,                    │                    │
  │                       short_able)                    │                    │
  │                                                      │                    │
  │                                              U100 (候选池)               │
  │                                               ├──► MC: DQN 选股范围      │
  │                                               └──► FC: 开新仓标的范围    │
  │                                                                          │
  └──────────────────────────────────────────────────────────────────────────┘

  ┌─── C. 基本面数据 ────────────────────────────────────────────────────────┐
  │                                                                          │
  │  Polygon.io ────► Parquet (预计算) ──► 基本面因子 (PE, PB, ROE, EPS)     │
  │  (或 Yahoo         (季度更新)           │                                │
  │   fallback)                             └──────► MC: 因子融合 → RL 选股  │
  │                                                                          │
  │  SEC EDGAR ─────► GPT-4.1 (long ctx) ──► 关键财务指标 JSON              │
  │  (10-K/10-Q,       (60k tokens 解析)     │                               │
  │   8-K 速报)                              ├──────► MC: 基本面因子补充     │
  │                                          └──────► FC: 8-K 速报触发信号   │
  │                                                                          │
  └──────────────────────────────────────────────────────────────────────────┘

  ┌─── D. 新闻数据 ─────────────────────────────────────────────────────────┐
  │                                                                          │
  │  NewsAPI ──────┐                                                         │
  │  (70k+来源,    │    ┌────────────────────────────────────────────┐       │
  │   500条/天)    ├──► │  G1 快速过滤 (<1s)                         │       │
  │                │    │  去重 + 实体识别 + 来源检查 → 95% 淘汰     │       │
  │  Perigon ──────┤    └──────────────┬─────────────────────────────┘       │
  │  (金融专业,    │                   │                                     │
  │   高信噪比)    │                   ▼                                     │
  │                │    ┌────────────────────────────────────────────┐       │
  │  Nasdaq RSS ───┘    │  G2 智能分类 (<5s)                         │       │
  │  (熔断/暂停)        │  FinBERT → 情绪 (pos/neg/neu)             │       │
  │                     │  GPT-4.1 nano → 主题 + 紧急度 + 相关性    │       │
  │                     └──────────────┬─────────────────────────────┘       │
  │                                    │                                     │
  │                    ┌───────────────┼───────────────┐                     │
  │                    ▼               ▼               ▼                     │
  │              sentiment_score  importance_score  urgency                  │
  │              (情绪分数)        (重要性分数)      (紧急度)                 │
  │                    │               │               │                     │
  │                    ├───────────────┼───────────────┘                     │
  │                    │               │                                     │
  │                    │    ┌──────────▼──────────────────────┐              │
  │                    │    │  G3 深度分析 (<30s, ≤10篇/天)    │              │
  │                    │    │  触发: importance>0.7 & urgent   │              │
  │                    │    │  Claude Sonnet 4 全文阅读        │              │
  │                    │    │  → 持仓影响 ±X%                  │              │
  │                    │    │  → target/stop 建议 (Phase 2)    │              │
  │                    │    └──────────┬──────────────────────┘              │
  │                    │               │                                     │
  │                    ▼               ▼                                     │
  │              ┌─────────────────────────────┐                             │
  │              │  信号评分                     │                             │
  │              │  score = 情绪×重要度×紧急度   │                             │
  │              │  Tier-1 (>85) / 2 / 3        │                             │
  │              └──────────┬──────────────────┘                             │
  │                         │                                                │
  │                         ├──────► FC: 入场/调仓/平仓决策                   │
  │                         │                                                │
  │                    ┌────▼────┐                                           │
  │                    │ 同时沉淀 │                                           │
  │                    └────┬────┘                                           │
  │                         │                                                │
  │     SQLite news_events ◄┘                                                │
  │     (全量存储, G1/G2/G3 分级)                                             │
  │              │                                                           │
  │              ├──────► MC: 周度情绪聚合因子                                │
  │              └──────► 训练: 新闻-收益关联训练集                            │
  │                                                                          │
  │  ── 知识图谱传播 (新闻触发) ────────────────────────────────             │
  │  │ 公司图 (100节点, 500边)                                   │           │
  │  │ G2 输出某公司情绪 → 沿关系边传播:                          │           │
  │  │   供应链: 同向 / 竞争: 反向 / 同行业: 弱正                │           │
  │  │ → propagation_score (每家公司)                             │           │
  │  │        ├──────► MC: 因子融合补充                           │           │
  │  │        └──────► FC: 关联股信号放大                         │           │
  │  └────────────────────────────────────────────────────────────┘           │
  │                                                                          │
  └──────────────────────────────────────────────────────────────────────────┘

  ┌─── E. 宏观经济数据 ──────────────────────────────────────────────────────┐
  │                                                                          │
  │  FRED API ──────► Parquet (历史) ──► Z-score (252日) ──► 经济周期分类     │
  │  (19个指标,        + SQLite          (标准化,             (扩张/过热/     │
  │   8大维度)          (最新值)          [-3,+3])             滞胀/衰退/复苏)│
  │                                         │                     │          │
  │                                         │              ┌──────▼────────┐ │
  │                                         │              │MacroTiltEngine│ │
  │                                         │              │               │ │
  │  Polymarket ────► 事件概率 delta ────────┼────────────► │ 敏感度矩阵    │ │
  │  (top 30 宏观                           │              │ (11 GICS ×    │ │
  │   事件概率)                              │              │  19 指标)     │ │
  │                                         │              └──────┬────────┘ │
  │                                         │                     │          │
  │                                         │              sector_tilt (±3%) │
  │                                         │              style_tilt (V/G)  │
  │                                         │                     │          │
  │                                         │                     │          │
  │                                         ├──────► MC: 宏观覆盖层 (Tilt)   │
  │                                         └──────► 训练: reward shaping    │
  │                                                                          │
  │  ── 知识图谱传播 (宏观触发) ────────────────────────────────             │
  │  │ 宏观图 (30节点, 70有向边)                                 │           │
  │  │ Z-score >1.0 触发传播:                                    │           │
  │  │   impact = z × polarity × strength, 衰减 0.5, 深度 3     │           │
  │  │ → 行业 Tilt 补充 (0.6 规则 + 0.4 传播)                   │           │
  │  │        └──────► MC: MacroTiltEngine 融合                  │           │
  │  └────────────────────────────────────────────────────────────┘           │
  │                                                                          │
  └──────────────────────────────────────────────────────────────────────────┘

  ┌─── F. 内部数据 (系统产生) ──────────────────────────────────────────────┐
  │                                                                          │
  │  当前持仓状态 ──► 持仓快照 (ticker, weight, pnl, hold_days)              │
  │  (Alpaca 账户)     │                                                     │
  │                    ├──────► MC: 约束检查 (sector cap, position cap)       │
  │                    ├──────► FC: 退出管理 (TTL 倒计时 / target-stop 监控)  │
  │                    └──────► 决策 LLM: 当前 vs 建议组合对比               │
  │                                                                          │
  │  历史交易记录 ──► SQLite trades 表 ──► 绩效统计                          │
  │  (order_id,         │                   (Sharpe, 胜率, 回撤)             │
  │   trigger_source,   │                                                    │
  │   signal_score)     ├──────► 训练: RL reward 信号                        │
  │                     └──────► 训练: FC 退出模型 (ML校准 + RL动态退出)     │
  │                                                                          │
  │  模型权重 (.pt) ──► 周日训练产出                                          │
  │  (DQN, PPO/SAC,     │                                                    │
  │   ML校准器,          ├──────► MC: RL 推理 (选股+权重)                     │
  │   RL退出模型)        └──────► FC: 退出模型推理 (Phase 2)                  │
  │                                                                          │
  └──────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                  汇入决策 — MC / FC 各自收到什么
═══════════════════════════════════════════════════════════════════════════════

  ┌─── MC 决策 LLM (周度, 周五 16:00) ──────────────────────────────────────┐
  │                                                                          │
  │  输入:                                                                   │
  │  ├─ 因子融合分数 (100只 × 100D)  ← A 技术因子 + C 基本面 + D 情绪       │
  │  │                                  + D propagation + E MacroTilt        │
  │  ├─ RL 推理结果                   ← DQN: 8只选股 + PPO: 权重分配        │
  │  ├─ 宏观覆盖层                    ← E sector_tilt + style_tilt          │
  │  ├─ 当前持仓快照                  ← F 持仓状态                           │
  │  ├─ 宏观环境摘要                  ← E 经济周期 + Polymarket 概率变化     │
  │  └─ 周度新闻摘要                  ← D news_events 聚合                   │
  │                                                                          │
  │  处理: 验证层 (Schema + 风控约束 + 异常检测)                              │
  │                                                                          │
  │  输出:                                                                   │
  │  └─ 建议组合 (8L+1S) + 权重 + 调仓理由 → Victor 审批 → Alpaca 执行      │
  │                                                                          │
  └──────────────────────────────────────────────────────────────────────────┘

  ┌─── FC 决策 (实时, 新闻触发) ────────────────────────────────────────────┐
  │                                                                          │
  │  输入:                                                                   │
  │  ├─ G2/G3 分析结果                ← D 情绪 + 主题 + 紧急度 + 影响评估   │
  │  ├─ 信号评分 + Tier 分级           ← D score = 情绪×重要度×紧急度        │
  │  ├─ 传播信号                      ← D 公司图 propagation_score           │
  │  ├─ 当前持仓快照                  ← F 持仓状态 + TTL/target/stop 状态    │
  │  ├─ 实时价格                      ← A 价格监控                           │
  │  └─ 退出模型推理 (Phase 2)        ← F ML 校准 target/stop + RL hold/sell │
  │                                                                          │
  │  处理: 约束检查 (U100, 5%单笔, 15%总量, 2次/天, 冷却期)                  │
  │                                                                          │
  │  输出:                                                                   │
  │  ├─ Tier-1: 自动执行 (买入/卖出/调仓) → 事后通知 Victor                  │
  │  ├─ Tier-2: 建议 + 理由 → Victor 15min 审批 → 执行/拒绝                 │
  │  └─ Tier-3: 记录观察列表                                                 │
  │                                                                          │
  └──────────────────────────────────────────────────────────────────────────┘

  ┌─── 后台训练 (周日 10:00) ──────────────────────────────────────────────┐
  │                                                                          │
  │  输入:                                                                   │
  │  ├─ 历史 OHLCV + Alpha158 因子    ← A (Parquet, 5-10年)                 │
  │  ├─ 历史宏观 Z-score + 周期标签   ← E (Parquet)                         │
  │  ├─ 历史交易记录 + 实现收益       ← F (SQLite)                           │
  │  └─ FC 交易记录 (Phase 2)         ← F (退出模型训练集)                   │
  │                                                                          │
  │  处理: DQN+PPO/SAC 训练 (16h full / 2h incremental)                      │
  │        + ML校准器 refit + RL退出模型训练 (Phase 2)                        │
  │                                                                          │
  │  输出:                                                                   │
  │  └─ 模型权重 (.pt) → MC + FC 共用                                        │
  │                                                                          │
  └──────────────────────────────────────────────────────────────────────────┘
```

### 数据源 × Pipeline 流向速查

| 数据源 | 原始格式 | 处理方式 | → MC (周度) | → FC (实时) | → 训练 (周日) |
|--------|---------|---------|-------------|-------------|---------------|
| **Alpaca OHLCV** | REST API | Parquet 存储 → Alpha158 表达式引擎 | 158 技术因子 | 实时价格 + 3σ 防守 | RL 环境状态 |
| **Alpaca Universe** | REST API | SQLite → 四层漏斗 | U100 候选池 | U100 开新仓范围 | — |
| **Polygon / Yahoo** | REST API | Parquet 预计算 | PE/PB/ROE 因子 | — | — |
| **SEC EDGAR** | Filing 全文 | GPT-4.1 解析 → JSON | 基本面因子 | 8-K 速报触发 | — |
| **NewsAPI** | headline+snippet | G1→G2→G3 三级漏斗 | 周度情绪聚合 | 信号评分 → Tier | 新闻-收益训练集 |
| **Perigon** | 结构化新闻 | G1→G2 (高信噪比) | — | 信号评分 → Tier | — |
| **Nasdaq RSS** | 事件流 | G1 直接触发 | — | 流动性事件 | — |
| **FRED** | 时间序列 | Z-score + MacroTiltEngine | sector/style tilt | — | reward shaping |
| **Polymarket** | 事件概率 | delta 检测 | MacroTilt 补充 | — | — |
| **公司图** | CSV → NetworkX | 新闻传播引擎 | propagation 因子 | 关联股信号放大 | — |
| **宏观图** | CSV → NetworkX | 冲击传播引擎 | 行业 Tilt 融合 | — | — |
| **持仓状态** | Alpaca 账户 | 实时快照 | 约束检查 | 退出管理 | — |
| **交易记录** | SQLite | 绩效统计 | — | — | RL reward + 退出模型 |

### MC 与 FC 交互

```
MC 周度重算时评估 FC 仓位:
  FC 新仓被 MC 选中 → 并入 MC, TTL/target/stop 取消
  FC 新仓未被选中   → 按 FC 退出策略处理
  FC 调仓 MC 持仓   → MC 重新计算权重, FC 调整归位
  3σ 防守触发       → 立即平仓 (MC/FC 均适用, 优先级最高)
```

---

## 6. 文档索引

本技术设计文档已拆分为以下子文档：

| 文档 | 内容 | 对应原始章节 |
|------|------|-------------|
| [00-architecture-overview.md](00-architecture-overview.md) | 系统架构总览、Pipeline、数据源 | §1 |
| [01-data-layer.md](01-data-layer.md) | 数据层：漏斗、存储、缓存、Provider | §2 |
| [02-model-layer.md](02-model-layer.md) | 模型层：LLM 路由、小模型、RL、MacroTilt | §3 |
| [03-news-processing.md](03-news-processing.md) | 信息源与新闻处理：G1/G2/G3、Polymarket | §4 |
| [04-decision-execution.md](04-decision-execution.md) | 决策与执行：再平衡、实时监控、风控 | §5-§6 |
| [05-infrastructure.md](05-infrastructure.md) | 基础设施、预算、安全、扩展性 | §7-§10 |
| [06-adr.md](06-adr.md) | 关键技术决策记录 (ADR) | §11 |
